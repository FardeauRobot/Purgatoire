# Run

Assumes you already ran `make` and produced `so_long`.

## Launch
- `./so_long maps/<map.ber>`

## Exit codes
- `0` on success
- `1` on invalid arguments or map errors
- `>0` if runtime fails (e.g., assets/window init)
